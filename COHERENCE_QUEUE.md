# HM Hyperpart coherence queue

Auto-generated by `scripts/hm_coherence_queue.py --write`.
Do not hand-edit — re-run after a sweep or drain.

Generated: 2026-07-13T20:49Z
Source: `.dazzle/hm-hyperpart-coherence/coherence.json`
Sweep: n=90 coherent=80 incoherent=10 mean=8.23

Consumed by `/improve hm-convergence hyperpart_coherence` **drain** phase.

| pri | stem | score | sev | category | description | action |
|----:|------|------:|-----|----------|-------------|--------|
| -59 | `grid-cols` | 1 | high | empty_demo | Screenshot is pure white (1280×900, single color, ~5KB) — no page chrome, title, | Investigate capture vs live page for `grid-cols` (file:// pa |
| -59 | `grid-edit` | 1 | high | empty_demo | Screenshot is pure white (1280×900, single color, ~5KB) — no page chrome, title, | Investigate capture vs live page for `grid-edit` (file:// pa |
| -59 | `grid-resize` | 1 | high | empty_demo | Screenshot is pure white (1280×900, single color, ~5KB) — no page chrome, title, | Investigate capture vs live page for `grid-resize` (file://  |
| -56 | `diagram` | 4 | high | empty_demo | Demo shows raw Mermaid/erDiagram source text inside a pre-like box instead of a  | Investigate capture vs live page for `diagram` (file:// pack |
| -56 | `pdf` | 4 | high | empty_demo | Toolbar chrome is present but canvas shows 'Failed to load document.' with empty | Investigate capture vs live page for `pdf` (file:// packages |
| -16 | `app-shell` | 4 | high | overflow | Main workspace copy is clipped under/against the left sidebar—sentence starts mi | CSS/demo polish for `app-shell` under packages/hatchi-maxchi |
| -15 | `radar` | 5 | high | missing_content | Demo is only a filled triangle polygon; polar axes, spoke scale, and axis labels | Investigate capture vs live page for `radar` (file:// packag |
| 6 | `related-tables` | 6 | high | overflow | Invoice status cards clip primary amounts/meta (e.g. £1,250 appears truncated) w | CSS/demo polish for `related-tables` under packages/hatchi-m |
| 31 | `message` | 6 | medium | spacing | Same meta collision as scroller: 'Maya Reyes10:02' and '10:04You' with no separa | CSS/demo polish for `message` under packages/hatchi-maxchi/  |
| 31 | `message-scroller` | 6 | medium | spacing | Author and timestamp meta run together with no gap: 'Maya Reyes10:02' and '10:04 | CSS/demo polish for `message-scroller` under packages/hatchi |
| 42 | `tags` | 7 | medium | missing_content | Demo shows the native comma-joined text field (urgent,backend) rather than the m | Investigate capture vs live page for `tags` (file:// package |
| 42 | `tree` | 7 | medium | missing_content | Engineering is expanded (down chevron, count chip 2) but no indented child rows  | Investigate capture vs live page for `tree` (file:// package |

## Loop

1. **Investigate** (if no `coherence.json` or ≥20 cycles stale): `hm_pages_vision.py --capture --all-hyperparts` + subagent batches + ingest
2. **Queue**: `python scripts/hm_coherence_queue.py --top 5`
3. **Drain** top candidate → fix HM → re-capture `--stems <stem>` → re-score
4. Seed backlog: `python scripts/hm_coherence_queue.py --seed-backlog`
